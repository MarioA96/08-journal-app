import {render} from '@testing-library/react'


describe('Prueba demo para verificar que las pruebas se realicen', () => {
    test('Esta prueba no debe fallar', () => {
        expect(1).toBe(1);
    })

})
